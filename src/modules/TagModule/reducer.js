// import {  } from './actions'

const initialState = {
  
}

function Reducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state
    }
}
  
export default Reducer